module.exports = {
    mongoURI: 'mongodb://[username:password@]host1[:port1][,host2[:port2],...[,hostN[:portN]]][/[database][?options]]'
};

//This is just a mock file to present how the db credentials are stored in the project.
//Actual data is in db.js, which has identical structure and is not pushed to the repo.